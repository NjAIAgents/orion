### Fixed
- The CI fix loop now tells a sandbox-policy denial apart from an agent that could not see the fix: when the sandbox refuses the agent's only edit (for example `.github/workflows/**`), Orion reports "blocked by policy" naming the tool, path and matched rule, hands off the agent's own diagnosis in full instead of discarding it, and does not spend the denied run against the three-attempt fix budget.
