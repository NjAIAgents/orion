### Fixed
- QA's full findings now reach the event log every round, not only when the round ceiling escalates to a person — so the common case (fixed in round one) leaves a durable record instead of nothing. The console line now shows the first substantive line of the findings (skipping a header like "Verification done. Summary:") and says when the full text is in the event log. Findings are also posted to the ticket on every round, not only at the ceiling.
