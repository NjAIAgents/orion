### Fixed
- The CI fix loop's activity now goes through the same attributed logger as every other supervised run, instead of a hand-rolled callback: console lines carry the ticket key, the devops actor and the model, and every tool call is written to the event log again (it was previously not recorded at all).
