### Fixed
- A squash-merged ticket no longer leaves its worktree and local branch behind. With `delete_branch_on_merge` active the branch's commits are reachable from no remote ref, so the unpushed-commits guard warned about work the forge had already accepted; that guard now applies only to prunes without a merged-PR verdict, where it still protects genuinely unpushed commits.
