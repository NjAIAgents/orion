### Fixed
- CI no longer runs twice for a push whose branch also has an open PR. The scaffolded workflow's concurrency group now keys on the PR number (falling back to the ref for a plain push), collapsing the push and pull_request triggers instead of running the full three-OS matrix under both.
