### Fixed
- The loop breaker no longer trips from parallel fan-out. A subagent shares its parent's session id but writes to its own transcript file; the identical-call counter now keys on that transcript file instead of the shared session, so two agents each innocently reading the same file no longer sum into a false `breaker/loop` trip that kills the run.
