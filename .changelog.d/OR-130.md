### Added

- A worktree can be locked for manual work by dropping a `.orion-manual-lock` file in it; both the CI fix loop and the auto-rebase path back off entirely while it is present, so a person resolving a real conflict by hand no longer races an unattended agent force-pushing the same branch (OR-130).
