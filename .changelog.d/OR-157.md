### Changed
- The CI fix loop now requires the agent to state a root cause, distinct from the failure log, before it writes a patch -- a symptom-derived fix is the most common way a fix attempt gets spent without moving the ticket forward, and the loop only gets three per ticket.
