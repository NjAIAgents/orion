### Added

- **A database architect takes part in planning.** It recommends a database with its
  reasoning, waits for that recommendation to be confirmed, and only then designs the
  schema -- so a choice nobody agreed to never becomes a premise for the work that
  follows. Also available directly as a performance and indexing reviewer.
